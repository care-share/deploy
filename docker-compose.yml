careshare:
  env_file: ./common.env
  build: ./careshare/

careauth:
  env_file: ./common.env
  build: ./careauth/
  ports:
    - "80:80"
  links:
    - careshare
    - openid
    - fhir
    - mongo

openid:
  env_file: ./common.env
  build: ./vha-rural-health-openid-connect-overlay/

fhir:
  env_file: ./common.env
  build: ./hapi-docker/

mongo:
  image: mongo:3

